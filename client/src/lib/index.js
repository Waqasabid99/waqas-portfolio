/**
 * lib/seo/index.ts
 * ─────────────────────────────────────────────
 * Barrel export — import everything from "@/lib/seo"
 *
 * Example:
 *   import {
 *     generateMovieMetadata,
 *     MovieJsonLd,
 *     SITE,
 *   } from "@/lib/seo";
 */

export * from "./seo.config";
export * from "./generateMetadata";
export * from "./jsonLd";
